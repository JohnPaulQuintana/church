<?php

     $conn= mysqli_connect('localhost', 'root','','db_delete_modal') or die(mysqli_error());
                    if(!$conn){
                       die("error: field to connect database"); 
                    }

     ?>